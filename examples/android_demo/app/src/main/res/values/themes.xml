<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Minimal theme - Centered engine handles all rendering -->
    <style name="Theme.CenteredDemo" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Transparent system bars for edge-to-edge rendering -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
